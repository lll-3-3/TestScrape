const http=require('http'),https=require('https');const PROVIDER_NAME='Cineby',SITE_URL='https://www.cineby.at',UA='Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/128.0.0.0 Safari/537.36',HEADERS={'User-Agent':UA,'Accept-Language':'en-US,en;q=0.9','Referer':SITE_URL};function clean(v){return(v||'').toString().replace(/\s+/g,' ').trim()}function makeUrl(path){return new URL(path,SITE_URL).toString()}function fetchText(url){return new Promise((resolve,reject)=>{const client=url.startsWith('https')?https:http;const req=client.get(url,{headers:HEADERS},res=>{if(res.statusCode>=300&&res.statusCode<400&&res.headers.location){resolve(fetchText(makeUrl(res.headers.location)));return}let data='';res.setEncoding('utf8');res.on('data',chunk=>data+=chunk);res.on('end',()=>resolve(data));});req.on('error',reject);});}function parseLinks(html){const out=[];const seen=new Set();const re=/<a[^>]+href=(['"])(.*?)\1[^>]*>(.*?)<\/a>/gi;for(const m of html.matchAll(re)){const href=clean(m[2]);const title=clean(m[3].replace(/<[^>]+>/g,''));if(!href||!title||href.startsWith('javascript:'))continue;const full=href.startsWith('http')?href:makeUrl(href);if(seen.has(full))continue;seen.add(full);if(!full.includes('/movie/')&&!full.includes('/tv/'))continue;out.push({href:full,title:title});}return out.slice(0,8);}function buildItem(href,title){const type=href.includes('/tv/')?'tv':'movie';return{id:'cineby:'+encodeURIComponent(href),title:title||'Cineby title',type:type,year:null,poster:SITE_URL+'/favicon.ico',url:href,provider:PROVIDER_NAME};}async function search(q){const query=clean(q);if(!query)return[];for(const url of [makeUrl(`/search?q=${encodeURIComponent(query)}`),makeUrl(`/search/${encodeURIComponent(query)}`),makeUrl(`/?s=${encodeURIComponent(query)}`),SITE_URL]){try{const html=await fetchText(url);const links=parseLinks(html);const found=links.filter(x=>x.title.toLowerCase().includes(query.toLowerCase())||query.toLowerCase().includes(x.title.toLowerCase())).map(x=>buildItem(x.href,x.title));if(found.length)return found;}catch(e){}}try{const html=await fetchText(SITE_URL);const links=parseLinks(html);if(links.length)return links.map(x=>buildItem(x.href,x.title));}catch(e){}return[{id:'cineby:'+encodeURIComponent(query.toLowerCase()),title:query,type:'movie',year:null,poster:SITE_URL+'/favicon.ico',url:makeUrl(`/search?q=${encodeURIComponent(query)}`),provider:PROVIDER_NAME}]}async function getStreams(item){const src=item&&(item.url||item.link||item.href||item.streamUrl||item.playUrl);if(!src)return[];const resolved=src.startsWith('http')?src:makeUrl(src);const play=resolved.includes('?play=true')?resolved:resolved+(resolved.includes('?')?'&':'?')+'play=true';return[{name:PROVIDER_NAME+' • Open in browser',url:play,quality:'Unknown',type:'direct',headers:{Referer:SITE_URL},subtitles:[],provider:PROVIDER_NAME,source:'site'}]}const manifest={id:'cineby',name:PROVIDER_NAME,description:'Cineby movie and TV streaming catalog provider',version:'1.0.0',author:'OpenAI',supportedTypes:['movie','tv'],filename:'providers/cineby.js',enabled:true,hasSettings:true,formats:['m3u8','mp4'],logo:SITE_URL+'/favicon.ico',contentLanguage:['en','ar']};module.exports={manifest,search,getStreams};
